# Upstream port assessment

## PR #28136 — direct lazy PLE reads

This is an open PR rather than a merged upstream commit. It is deliberately
not applied on this branch: its lazy-PLE changes require a manual integration
with drluoto's Strix Halo-specific Qwen3.8 code and a cold-prefill validation.
No benchmark is recorded and no performance claim is made.
